# Daily Notes (MAS 5.0): 2026-05-14

**1. Work Production Executed (Results)**
- Transitioned OAC BD Warm-Up Hub from Global scripts to Personal Scripts (`user_scripts` collection).
- Added Campaign Emails list feature for users with a 'Copy Emails' action.
- Added 'Draft in Gmail' feature (mailto link opening).
- Re-injected all UI Dashboard features (Account Rotation Tracker, Phase Progress Roadmap, Auto Launcher generator).
- Modified Firebase Authentication: Removed `@onearw.com` hard restriction to allow domain scaling, handling auth solely via `firestore.rules` and hardcoded admin list (`oac.vn@onearw.com`, `lauren.luu@onearw.com`, `ellie.tran@onearw.com`).
- Overhauled `firestore.rules` and successfully deployed code to `main` branch of `oacvn-bot/oac-bd-reminder`.
- Created comprehensive `user_guide.md` covering workflow for both Team Admins and standard Members.

**2. Contextual Intelligence & Synthesized Learnings (Tacit Knowledge)**
- Firebase configurations for this specific Vite applet are statically bundled via `firebase-applet-config.json` and committed directly to the repo. Therefore, Vercel deployments do NOT require `.env` configuration injection.
- Gmail API was explicitly bypassed for this sprint phase due to the complexity of OAuth/Google Cloud configuration, opting for immediate MVP value via the `mailto:` & clipboard injection workflow.

**3. Strategic Hand-over Queue (Outstanding Architecture)**
- [PENDING] Config Firebase rules + Send User Guide to Team (Admin must manually copy `firestore.rules` content into the Firebase Console UI -> Rules -> Publish. Then share the newly generated `user_guide.md` with the OAC BD team.)
